services:
  mariadb:
    build: requirements/mariadb
    networks:
      - wordpress-network
    volumes:
      - wordpress-volume:/var/html/
  wordpress:
    build: requirements/wordpress
    networks:
      - wordpress-network
    volumes:
      - wordpress-volume:/var/html/
    depends_on:
      - mariadb
  nginx:
    build: requirements/nginx
    ports:
      - "443:443"
    networks:
      - wordpress-network
    volumes:
      - mariadb-volume:/var/mariadb
    depends_on:
      - wordpress
volumes:
  mariadb-volume:
    driver: local
  wordpress-volume:
    driver: local
networks:
  wordpress-network: {}
